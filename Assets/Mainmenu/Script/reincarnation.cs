using UnityEngine;

public class reincarnation : MonoBehaviour
{
    int reincarnationcount = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void reset()
    {
        reincarnationcount++;
    }
}
